//Master Code

const source = require("./source");  //Getting the source code from the source file

console.log(source.sum(1, 5)); // putting the sum variable in the console
console.log(source.PI); // putting the PI variable in the console
console.log(new source.someMathObj()); // putting the someMathObj object into the console
